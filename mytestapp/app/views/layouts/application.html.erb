<!DOCTYPE html>
<html>
<head>
  <title>Mytestapp</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div align="center">
  <div>
  <%#=admin.inspect if current_user && admin%>
  	<% if !current_user.nil? %>
  	Welcome <%= current_user.user_name %> <%= link_to "Sign out", logout_path, :method => :delete %>
  	 <%pvalue=Profile.find_by_user_id(current_user.user_id)%>
     <strong><%= link_to "Home", root_path%></strong>
  	 <strong><%= link_to "Profile", profile_path(pvalue.id)%></strong>
     <strong><%= link_to "Products", items_path%></strong>
  	
      <% else %>
         <%= link_to "Sign in", login_path %>	
      <% end %>
  </div>
  <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, class: "alert alert-info" %>
  <% end %>
  <%= yield %>
</div>
</body>
</html>

